---
title: v1.131
description: "Enable Authentication via QR Code on Mobile except iOS app and combined price columns in order history."
---

<Callout intent="info">
  **🟢 Prod: Feb 3rd–Feb 4th, 2025** — Enable Authentication via QR Code on Mobile except iOS app and combined price columns in order history.
</Callout>

<Accordion title="v1.131.2 — Feb 4th" defaultOpen={true}>

### 🖥️ UI Updates
* Enable Authentication via QR Code on Mobile except iOS app.
* Combined `Price` and `AvgFillPrice` columns in Order History.

<Callout intent="note">
  **🟡 Testnet (v1.131.2-rc.1): Feb 4th** — Same changes, same date.
</Callout>

</Accordion>

<Accordion title="v1.131.1 — Feb 3rd" defaultOpen={false}>

### ⚙️ Engine Updates
* `starknet_gateway_url` field in the GET `/system/config` endpoint has been deprecated.
* During maintenance (`POST_ONLY`), the platform will reject aggressive orders placed outside a ±5 bps band from the `external fair price`.

</Accordion>

<Callout intent="note">
  **🟡 Testnet (v1.131.0-rc.1): Jan 30th** — Testnet-only changes:
  * `starknet_gateway_url` field in the GET `/system/config` endpoint has been deprecated.
  * During maintenance (`POST_ONLY`), the platform will reject aggressive orders placed outside a ±5 bps band from the `external fair price`.
</Callout>
