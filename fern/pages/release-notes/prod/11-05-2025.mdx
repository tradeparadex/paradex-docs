## v1.119.3

<ChangelogTags tags={["UI", "API"]} />

* **UI:** Always show unaggregated spreads, including `RPI` prices.
* **UI:** Increased the number of visible rows in the `Trade History` table.
* **UI:** Added a new tab on the `XP` page to view the `live` feed of public `XP transfers`.
* **API:** Included `relative_spread` in WebSocket `orderbook` updates.
* **API:** Anonymize `Public XP` transfers.
