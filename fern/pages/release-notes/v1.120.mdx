---
title: v1.120
description: "Trigger sound effect when there's a fill, can be disabled via notification setting."
---

<Callout intent="info">
  **v1.120 — Nov 2025**

  Trigger sound effect when there's a fill, can be disabled via notification setting.
</Callout>

<Accordion title="v1.120.3 — Trigger sound effect when there's a fill, can be disabled via" defaultOpen={true}>

  <Badge color="green">PROD  Nov 12th</Badge> <Badge color="yellow">TESTNET  Nov 12th</Badge>

  **🖥️ UI Updates**
  - Trigger sound effect when there's a fill, can be disabled via notification setting.

</Accordion>

<Accordion title="v1.120.1 — Market summary now includes historically populated open_interest data" defaultOpen={false}>

  <Badge color="green">PROD  Nov 11th</Badge> <Badge color="yellow">TESTNET  Nov 11th</Badge>

  **⚙️ Engine Updates**
  - Market summary now includes historically populated `open_interest` data.

</Accordion>

<Accordion title="v1.120.0 — TPSL Modal can now be dragged and repositioned on screen for better" defaultOpen={false}>

  <Badge color="green">PROD  Nov 10th</Badge> <Badge color="yellow">TESTNET  Nov 7th</Badge>

  **🖥️ UI Updates**
  - `TPSL` Modal can now be dragged and repositioned on screen for better visibility.

  **⚙️ Engine Updates**
  - Fixed issue where all `TWAP` orders were canceled if any sub-order was canceled due to `exceeded slippage`.
  - Disabled maximum slippage check for `LIMIT` orders.
  - Fixed race condition when modifying `TP/SL` orders that could cause the account to become stuck and prevent new order creation.

</Accordion>
