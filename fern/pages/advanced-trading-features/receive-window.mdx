---
title: Receive Window
---

The `recv_window` parameter specifies the number of milliseconds the signature timestamp is valid after the submission time. If not specified, this is currently set to 300000 ms (5 minute) by default.

`Condition : If current_time > recv_window + signature_time → reject the order`
