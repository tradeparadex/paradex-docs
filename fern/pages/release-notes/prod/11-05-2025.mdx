## v1.119.3

<ChangelogTags tags={["UI", "API"]} />

* Always show unaggregated spreads, including `RPI` prices.
* Increased the number of visible rows in the `Trade History` table.
* Added a new tab on the `XP` page to view the `live` feed of public `XP transfers`.
* Included `relative_spread` in WebSocket `orderbook` updates.
* Anonymize `Public XP` transfers.
