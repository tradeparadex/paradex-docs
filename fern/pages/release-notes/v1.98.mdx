---
title: v1.98
description: "Mobile: and updated navigation from markets page to land on charts page."
---

<Callout intent="info">
  **🟢 Prod: June 2nd–June 12th, 2025** — Mobile: and updated navigation from markets page to land on charts page.
</Callout>

<Accordion title="v1.98.11 — June 12th" defaultOpen={true}>

### 🖥️ UI Updates
* Mobile:
* Updated Navigation from `Markets` page to land on `Charts` page.

</Accordion>

<Accordion title="v1.98.9 — June 6th" defaultOpen={false}>

### 🖥️ UI Updates
* Mobile:
* Added highlight/indication on orderbook for crossed `RPI` orders on the `Chart` Page.
* Performance improvements for the `Markets` menu dropdown `open/close` interaction.
* Added ability to `Cancel` All `Buy/Sell` orders in `Open Orders` tab

<Callout intent="note">
  **🟡 Testnet (v1.98.9-rc.1): June 6th** — Same changes, same date.
</Callout>

</Accordion>

<Accordion title="v1.98.7 — June 5th" defaultOpen={false}>

### 🖥️ UI Updates
* Enabled scrolling for `Trade Tape` on `Desktop`.
* `RPI` indication on Trade History tab.

</Accordion>

<Accordion title="v1.98.6 — June 5th" defaultOpen={false}>

### 🖥️ UI Updates
* Mobile - `Deposit/Withdraw` : fixed non-scrollable chain select dropdown.

### ⚙️ Engine Updates
* Added `flags` on `GET /fills` API and WebSocket to indicate `RPI` fills.

<Callout intent="note">
  **🟡 Testnet (v1.98.6-rc.1): June 5th** — Same changes, same date.
</Callout>

</Accordion>

<Accordion title="v1.98.4 — June 3rd" defaultOpen={false}>

### 🖥️ UI Updates
* `USDC` Deposits from `Solana` Now Available in UI
* Now deposit USDC directly from the `Solana` through the Paradex UI.
* Connect your `Solana` wallet to seamlessly transfer USDC to your trading account

<Callout intent="note">
  **🟡 Testnet (v1.98.4-rc.1): June 3rd** — Same changes, same date.
</Callout>

</Accordion>

<Accordion title="v1.98.2 — June 3rd" defaultOpen={false}>

### ⚙️ Engine Updates
* Added support for `Retail Price Improvement` orders for enhanced liquidity and improved pricing.

### 🖥️ UI Updates
* `RPI` highlight crossed order book rows indicating `Retail Price Improvement`.

<Callout intent="note">
  **🟡 Testnet (v1.98.2-rc.1): June 2nd** — Same changes.
</Callout>

</Accordion>

<Accordion title="v1.98.0 — June 2nd" defaultOpen={false}>

### ⚙️ Engine Updates
* `POST_ONLY` orders are not subject to `MAX_SLIPPAGE` checks.

<Callout intent="note">
  **🟡 Testnet (v1.98.0-rc.2): May 31st** — Same changes.
</Callout>

</Accordion>
