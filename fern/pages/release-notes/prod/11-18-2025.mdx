## v1.121.2

<ChangelogTags tags={["UI", "API"]} />

* **UI:** Include `Funding` and `Realized PnL` for `Liquidation Fills`.
* **UI:** Use `best bid/offer` for `MAX_SLIPPAGE` calculation for mark_price.
* **UI:** Orders from UI - use a new request field `max_slippage_price` for `Market` orders based on calculated slippage price against `BBO`
* **API:** `TP/SL` orders are not subject to max user defined slippage - These orders now use `price_bands` as configured for individual markets.
