---
title: v1.131
description: "Enable Authentication via QR Code on Mobile except iOS app and combined price columns in order history."
---

<Callout intent="info">
  **v1.131 — Feb 2025**

  Enable Authentication via QR Code on Mobile except iOS app and combined price columns in order history.
</Callout>

<Accordion title="v1.131.2 — Enable Authentication via QR Code on Mobile except iOS app" defaultOpen={true}>

  <Badge color="green">PROD  Feb 4th</Badge> <Badge color="yellow">TESTNET  Feb 4th</Badge>

  **🖥️ UI Updates**
  - Enable Authentication via QR Code on Mobile except iOS app.
  - Combined `Price` and `AvgFillPrice` columns in Order History.

</Accordion>

<Accordion title="v1.131.1 — starknet_gateway_url field in the GET /system/config endpoint has" defaultOpen={false}>

  <Badge color="green">PROD  Feb 3rd</Badge> <Badge color="yellow">TESTNET  Feb 3rd</Badge>

  **⚙️ Engine Updates**
  - `starknet_gateway_url` field in the GET `/system/config` endpoint has been deprecated.
  - During maintenance (`POST_ONLY`), the platform will reject aggressive orders placed outside a ±5 bps band from the `external fair price`.

</Accordion>
