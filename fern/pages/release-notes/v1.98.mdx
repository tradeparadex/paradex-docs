---
title: v1.98
description: "Mobile: and updated navigation from markets page to land on charts page."
---

<Callout intent="info">
  **v1.98 — June 2025**

  Mobile: and updated navigation from markets page to land on charts page.
</Callout>

<Accordion title="v1.98.11 — Mobile:" defaultOpen={true}>

  <Badge color="green">PROD  Jun 12th</Badge> <Badge color="yellow">TESTNET  Jun 12th</Badge>

  **🖥️ UI Updates**
  - Mobile:
  - Updated Navigation from `Markets` page to land on `Charts` page.

</Accordion>

<Accordion title="v1.98.9 — Mobile:" defaultOpen={false}>

  <Badge color="green">PROD  Jun 6th</Badge> <Badge color="yellow">TESTNET  Jun 6th</Badge>

  **🖥️ UI Updates**
  - Mobile:
  - Added highlight/indication on orderbook for crossed `RPI` orders on the `Chart` Page.
  - Performance improvements for the `Markets` menu dropdown `open/close` interaction.
  - Added ability to `Cancel` All `Buy/Sell` orders in `Open Orders` tab

</Accordion>

<Accordion title="v1.98.7 — Enabled scrolling for Trade Tape on Desktop" defaultOpen={false}>

  <Badge color="green">PROD  Jun 5th</Badge> <Badge color="yellow">TESTNET  Jun 5th</Badge>

  **🖥️ UI Updates**
  - Enabled scrolling for `Trade Tape` on `Desktop`.
  - `RPI` indication on Trade History tab.

</Accordion>

<Accordion title="v1.98.6 — Mobile - Deposit/Withdraw : fixed non-scrollable chain select dropdown" defaultOpen={false}>

  <Badge color="green">PROD  Jun 5th</Badge> <Badge color="yellow">TESTNET  Jun 5th</Badge>

  **🖥️ UI Updates**
  - Mobile - `Deposit/Withdraw` : fixed non-scrollable chain select dropdown.

  **⚙️ Engine Updates**
  - Added `flags` on `GET /fills` API and WebSocket to indicate `RPI` fills.

</Accordion>

<Accordion title="v1.98.4 — USDC Deposits from Solana Now Available in UI" defaultOpen={false}>

  <Badge color="green">PROD  Jun 3rd</Badge> <Badge color="yellow">TESTNET  Jun 3rd</Badge>

  **🖥️ UI Updates**
  - `USDC` Deposits from `Solana` Now Available in UI
  - Now deposit USDC directly from the `Solana` through the Paradex UI.
  - Connect your `Solana` wallet to seamlessly transfer USDC to your trading account

</Accordion>

<Accordion title="v1.98.2 — Added support for Retail Price Improvement orders for enhanced" defaultOpen={false}>

  <Badge color="green">PROD  Jun 3rd</Badge> <Badge color="yellow">TESTNET  Jun 2nd</Badge>

  **⚙️ Engine Updates**
  - Added support for `Retail Price Improvement` orders for enhanced liquidity and improved pricing.

  **🖥️ UI Updates**
  - `RPI` highlight crossed order book rows indicating `Retail Price Improvement`.

</Accordion>

<Accordion title="v1.98.0 — POST_ONLY orders are not subject to MAX_SLIPPAGE checks" defaultOpen={false}>

  <Badge color="green">PROD  Jun 2nd</Badge> <Badge color="yellow">TESTNET  May 31st (rc.2)</Badge>

  **⚙️ Engine Updates**
  - `POST_ONLY` orders are not subject to `MAX_SLIPPAGE` checks.

</Accordion>
