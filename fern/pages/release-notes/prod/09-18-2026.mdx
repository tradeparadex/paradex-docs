## v1.163.9

<ChangelogTags tags={["UI"]} />

#### UI

* Closed options, spot and multi-leg trades can now be shared as P&L cards.
* Grouped positions no longer split an options structure when its delta hedge nets against an existing perp position.
