---
title: v1.131
---
## Prod
### v1.131.2 - Feb 4th
🖥️ UI Updates
* Combined `Price` and `AvgFillPrice` columns in Order History.

⚙️ API Updates
* Handle 0 price bands width scenarios. User can input maximum slippage (capped at 10000% by backend).
* Handle decimal places for price in limit Sell modal.
* Slightly improved error handling for deposits and withdrawals.
---
### v1.131.1 - Feb 3rd
⚙️ API Updates
* `starknet_gateway_url` field in the GET `/system/config` endpoint has been deprecated.
* During maintenance (`POST_ONLY`), the platform will reject aggressive orders placed outside a ±5 bps band from the `external fair price`.


## Testnet
### v1.131.2-rc.1 - Feb 4th
🖥️ UI Updates
* Combined `Price` and `AvgFillPrice` columns in Order History.

⚙️ API Updates
* Handle 0 price bands width scenarios. User can input maximum slippage (capped at 10000% by backend).
* Handle decimal places for price in limit Sell modal.
* Slightly improved error handling for deposits and withdrawals.
---
### v1.131.0-rc.1 - Jan 30th
⚙️ API Updates
* `starknet_gateway_url` field in the GET `/system/config` endpoint has been deprecated.
* During maintenance (`POST_ONLY`), the platform will reject aggressive orders placed outside a ±5 bps band from the `external fair price`.
---
