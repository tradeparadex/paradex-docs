---
title: v1.120
---
## Prod
### v1.120.1 - Nov 11th
⚙️ API Updates
* Add `node_id` on ws auth response to identify node you are connected to.
* `open_interest` is populated historically in market summary ws channel.
---
### v1.120.0 - Nov 10th
🖥️ UI Updates
* `TPSL` Modal can now be dragged and repositioned on screen for better visibility.

⚙️ API Updates
* Fixed issue where all `TWAP` orders were cancelled if any sub-order was cancelled due to `exceeded slippage`.
* Disabled maximum slippage check for `LIMIT` orders.
* Fixed race condition when modifying `TP/SL` orders that could cause the account to become stuck and prevent new order creation.
---

## Testnet
### v1.120.1-rc.1 - Nov 11th
⚙️ API Updates
* Add `node_id` on ws auth response to identify node you are connected to.
* `open_interest` is populated historically in market summary ws channel.
---
### v1.120.0-rc.1 - Nov 7th
🖥️ UI Updates
* `TPSL` Modal can now be dragged and repositioned on screen for better visibility.

⚙️ API Updates
* Fixed issue where all `TWAP` orders were cancelled if any sub-order was cancelled due to `exceeded slippage`.
* Disabled maximum slippage check for `LIMIT` orders.
* Fixed race condition when modifying `TP/SL` orders that could cause the account to become stuck and prevent new order creation.
---
