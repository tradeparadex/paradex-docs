## v1.155.0

<ChangelogTags tags={["UI"]} />

#### UI

* `Trade History` asset filters (Futures / Options / Spot) now load the selected kind's history from the server, so spot trades are no longer missing for active traders.

