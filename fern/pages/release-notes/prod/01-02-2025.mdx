## v1.75.0

<ChangelogTags tags={["UI", "API"]} />

* **UI:** Show Max Slippage in Stop Market, Edit TPSL and Market Close forms.
* **UI:** Add Realized P&L to Trade History table.
* **UI:** Improve visibility of TPSL column in Positions table.
* **UI:** User now can see depositors count on vault card on mobile. 
* **UI:** Age value removed from the mobile card.
* **UI:** Added USD estimation in Take Profit / Stop Loss modal.
* **API:** Add `realized_pnl` attribute to `/fills` API and to `fills.{market}` WebSocket channel. The attribute is calculated only for new trades.
