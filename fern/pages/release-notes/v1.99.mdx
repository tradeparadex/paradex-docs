---
title: v1.99
description: "Order Book - hide currency display on sizes, when USD is selected, and more."
---

<Callout intent="info">
  **June 16th–June 20th, 2025** — Order Book - hide currency display on sizes, when USD is selected, and more.
</Callout>

<Tabs>
  <Tab title="🟢 Prod">
      <Accordion title="v1.99.8 — June 20th" defaultOpen={true}>

      ### 🖥️ UI Updates
      * Order Book - hide currency display on sizes, when USD is selected
      * Order Book - Updated `aggregation` and `currency selector` dropdown styling.
      * Mobile
      * Enabled `Drawing Utils` on `Trading View`.

      </Accordion>

      <Accordion title="v1.99.7 — June 20th" defaultOpen={false}>

      ### 🖥️ UI Updates
      * Mobile: Contract details are now available in a separate tab.

      </Accordion>

      <Accordion title="v1.99.6 — June 19th" defaultOpen={false}>

      ### 🖥️ UI Updates
      * Trade Page: `Order Book`, `Trades` sections now have `Tabs` layout.

      </Accordion>

      <Accordion title="v1.99.5 — June 19th" defaultOpen={false}>

      ### 🖥️ UI Updates
      * Modify Order, Options: fix min notional validation.

      </Accordion>

      <Accordion title="v1.99.3 — June 18th" defaultOpen={false}>

      ### 🖥️ UI Updates
      * Mobile:
      * Back Button to cancel Buy or Sell.
      * Ability to update settings for TickSize and Currency on `Orderbook`.
      * XP History table now fills in missing weeks with zero values for a complete timeline view.

      </Accordion>

      <Accordion title="v1.99.2 — June 17th" defaultOpen={false}>

      ### 🖥️ UI Updates
      * Mobile: Performance improvements for interaction with `Markets`, `Trade Chart`, `Trade Orderbook` pages.

      ### ⚙️ Engine Updates
      * Reduced `Option Maker Fees` to `0`

      </Accordion>

      <Accordion title="v1.99.0 — June 16th" defaultOpen={false}>

      ### 🖥️ UI Updates
      * Bug fix for arrow indicator on `orderbook` with open order indicator showing for multiple markets if both markets have order with same price.

      </Accordion>

  </Tab>
  <Tab title="🟡 Testnet">
      <Accordion title="v1.99.8-rc.1 — June 20th" defaultOpen={false}>

      ### 🖥️ UI Updates
      * Order Book - hide currency display on sizes, when USD is selected
      * Order Book - Updated `aggregation` and `currency selector` dropdown styling.
      * Mobile
      * Enabled `Drawing Utils` on `Trading View`.

      </Accordion>

      <Accordion title="v1.99.7-rc.1 — June 20th" defaultOpen={false}>

      ### 🖥️ UI Updates
      * Mobile: Contract details are now available in a separate tab.

      </Accordion>

      <Accordion title="v1.99.6-rc.1 — June 19th" defaultOpen={false}>

      ### 🖥️ UI Updates
      * Trade Page: `Order Book`, `Trades` sections now have `Tabs` layout.

      </Accordion>

      <Accordion title="v1.99.4-rc.1 — June 19th" defaultOpen={false}>

      ### 🖥️ UI Updates
      * Modify Order, Options: fix min notional validation.

      </Accordion>

      <Accordion title="v1.99.3-rc.1 — June 17th" defaultOpen={false}>

      ### 🖥️ UI Updates
      * Mobile:
      * Back Button to cancel Buy or Sell.
      * Ability to update settings for TickSize and Currency on `Orderbook`.
      * XP History table now fills in missing weeks with zero values for a complete timeline view.

      </Accordion>

      <Accordion title="v1.99.2-rc.2 — June 16th" defaultOpen={false}>

      ### 🖥️ UI Updates
      * Mobile: Performance improvements for interaction with `Markets`, `Trade Chart`, `Trade Orderbook` pages.

      ### ⚙️ Engine Updates
      * Reduced `Option Maker Fees` to `0`

      </Accordion>

      <Accordion title="v1.99.0-rc.2 — June 16th" defaultOpen={false}>

      ### 🖥️ UI Updates
      * Bug fix for arrow indicator on `orderbook` with open order indicator showing for multiple markets if both markets have order with same price.

      </Accordion>

  </Tab>
</Tabs>
