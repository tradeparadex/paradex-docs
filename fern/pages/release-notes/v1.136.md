---
title: v1.136
---
## Prod
### v1.136.5 - March 11th
⚙️ API Updates
* TWAP API returns `interval_seconds` field.
* Expose `jwks` endpoint for `jwt` key validation.
---
### v1.136.3 - March 10th
⚙️ API Updates
* Only cancel LIMIT TP/SL when it's fully filled instead of triggered.
---
### v1.136.0 - March 9th
🖥️ UI Updates
* Enable Frequency selection on `TWAP` Order Builder.
* The order builder now shows a `0% Fee` badge when fees are zero, otherwise displays fees in a compact `T/M` format.
---

## Testnet
### v1.136.5-rc.1 - March 11th
⚙️ API Updates
* TWAP API returns `interval_seconds` field.
* Expose `jwks` endpoint for `jwt` key validation.
---
### v1.136.3-rc.1 - March 10th
⚙️ API Updates
* Only cancel LIMIT TP/SL when it's fully filled instead of triggered.
---
### v1.136.0-rc.1 - March 6th
🖥️ UI Updates
* Enable Frequency selection on `TWAP` Order Builder.
* The order builder now shows a `0% Fee` badge when fees are zero, otherwise displays fees in a compact `T/M` format.
---
