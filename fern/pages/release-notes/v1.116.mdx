---
title: v1.116
description: "Fixed leverage slider calculation for Options when using Spot Notional order value."
---

<Callout intent="info">
  **v1.116 — Oct 2025**

  Fixed leverage slider calculation for Options when using Spot Notional order value.
</Callout>

<Accordion title="v1.116.9 — Fixed leverage slider calculation for Options when using Spot" defaultOpen={true}>

  <Badge color="green">PROD  Oct 16th</Badge> <Badge color="yellow">TESTNET  Oct 16th</Badge>

  **🖥️ UI Updates**
  -  Fixed leverage slider calculation for `Options` when using `Spot Notional` order value

</Accordion>

<Accordion title="v1.116.3 — Added order slider for Stop Limit & Stop Market orders" defaultOpen={false}>

  <Badge color="green">PROD  Oct 15th</Badge> <Badge color="yellow">TESTNET  Oct 15th</Badge>

  **🖥️ UI Updates**
  -  Added order slider for `Stop Limit` & `Stop Market` orders.

</Accordion>

<Accordion title="v1.116.2 — Volume is now shown separately on TradingView charts for a cleaner UI" defaultOpen={false}>

  <Badge color="green">PROD  Oct 14th</Badge> <Badge color="yellow">TESTNET  Oct 14th</Badge>

  **🖥️ UI Updates**
  -  Volume is now shown separately on TradingView charts for a cleaner UI.
  -  Charts react instantly to updates in MarkPrice and LastPrice.

</Accordion>

<Accordion title="v1.116.1 — TP/SL profit/loss input mode ($/%) selection in Order Builder is now" defaultOpen={false}>

  <Badge color="green">PROD  Oct 13th</Badge> <Badge color="yellow">TESTNET  Oct 13th (rc.0)</Badge>

  **🖥️ UI Updates**
  -  `TP/SL profit/loss input mode ($/%)` selection in Order Builder is now saved and will persist across sessions.
  -  Added filtering functionality across all Perpetuals tabs — Positions, Risk, Open Orders, TWAP, Trade History, and Order History — enabling quicker search and management of specific trades.

</Accordion>

<Accordion title="v1.116.0 — Order Builder: Order size now updates dynamically with account value" defaultOpen={false}>

  <Badge color="green">PROD  Oct 13th</Badge> <Badge color="yellow">TESTNET  Oct 11th</Badge>

  **🖥️ UI Updates**
  - Order Builder: Order `size` now updates dynamically with account value and market price when using the leverage slider.
  - Display `ISO` tag on portfolio transfer table for `ISO` transaction.
  - The `Earn` dropdown menu has been replaced with a direct `Refer` link for easier access.
  - XP details are now accessible by clicking the `S2 XP` counter at the top of the page.
  - `Order Builder` now remembers your `Time In Force (GTC/IOC)` preference for each order type, automatically restoring your selection when you switch between order types.

</Accordion>
