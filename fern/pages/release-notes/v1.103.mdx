---
title: v1.103
description: "Mobile : Users can access positions directly from Trade Summary Page."
---

<Callout intent="info">
  **🟢 Prod: July 14th–July 17th, 2025** — Mobile : Users can access positions directly from Trade Summary Page.
</Callout>

<Accordion title="v1.103.6 — July 17th" defaultOpen={true}>

### 🖥️ UI Updates
*  Mobile : Users can access `positions` directly from `Trade Summary` Page.

<Callout intent="note">
  **🟡 Testnet (v1.103.6-rc.1): July 17th** — Same changes, same date.
</Callout>

</Accordion>

<Accordion title="v1.103.3 — July 16th" defaultOpen={false}>

### ⚙️ Engine Updates
*  Introduce per market `default max slippage`. Platform will be using default max slippage value, if user has not set the max slippage.
*  Default Slippage is calculated based on leverage:
*  `PERP` markets Leverage: `1/cross_margin_params.imf_base`
*  `PERP_OPTION` markets Leverage: `1/cross_margin_params.imf.long_itm`
*  Leverage based max slippage defaults:
* Exposed `default` market slippage on API `GET /markets` endpoint.
* Updated Tick sizes for multiple `PERP` markets as below

<Callout intent="note">
  **🟡 Testnet (v1.103.3-rc.1): July 15th** — Same changes.
</Callout>

</Accordion>

<Accordion title="v1.103.0 — July 14th" defaultOpen={false}>

### 🖥️ UI Updates
*  Show `Estimated P&L` in order builder `TPSL` form.

<Callout intent="note">
  **🟡 Testnet (v1.103.0-rc.1): July 11th** — Same changes.
</Callout>

</Accordion>
